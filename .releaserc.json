{
  "branches": [
    "main"
  ],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/npm",
      {
        "npmPublish": true,
        "pkgRoot": "."
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "echo \"Updating config.yaml version for Home Assistant addon compatibility\" && node scripts/update-ha-addon-version.js \"${nextRelease.version}\" && echo \"Running scripts/build.js to update generated version files...\" && node scripts/build.js && echo \"Staging changed files...\" && git add config.yaml && git add -f src/version.ts build/version.js"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["package.json", "config.yaml", "src/version.ts", "build/version.js"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [],
        "failComment": false,
        "failTitle": false
      }
    ]
  ]
}
